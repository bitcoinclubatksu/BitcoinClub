<style>

	h1, figure {
		text-align: center;
		margin: 0 auto;
	}

    .bottom {
        background: rgba(255,130,21,0.1);
    }

	h1 {
		color: #ff3e00;
		font-size: 4em;
		text-transform: uppercase;
		font-weight: 100;
		margin: 0 0 0.5em 0;
	}

	@media (min-width: 640px) {
		h1 {
			font-size: 4em;
		}
	}

	.container {
		position: relative;
	}

    .spin {
	    -webkit-filter: grayscale(0);
	    filter: grayscale(0) blur(0);
	    -webkit-transition: .3s ease-in-out;
	    transition: .3s ease-in-out;
    }

    .spin:hover {
    	-webkit-filter: grayscale(100%) blur(2px);
    	filter: grayscale(100%) blur(2px);
    } 

	.spin:hover .middle {
		opacity: 1;
	}

	.middle {
  		transition: .5s ease;
  		opacity: 0;
  		position: absolute;
  		top: 50%;
  		left: 50%;
  		transform: translate(-50%, -50%);
  		-ms-transform: translate(-50%, -50%);
  		text-align: center;
	}
	

	.price {
		font-size: 26px;
	}

</style>

<script>
    import { onMount } from 'svelte';
    import { fade } from 'svelte/transition';
    import getBTCPrice from './_btc.js';

    let btc;
    onMount(async () => {
       btc = parseFloat(await getBTCPrice()).toFixed(2);
    });
</script>


<svelte:head>
	<title>Bitcoin Club at KSU</title>
</svelte:head>

<h1>Bitcoin Club at KSU!</h1>

<div class="container">
	<figure>
		<img class="spin" alt='Bitcoin Club' src='bitcoin_spin.gif'/>
	</figure>
	<div class="middle">
		<div class="price">{btc}</div>
	</div>
</div>
